# Core scientific stack
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
pyyaml>=6.0

# Hugging Face stack
transformers>=4.40
datasets>=2.18
accelerate>=0.27
sentence-transformers>=2.6
tokenizers>=0.19

# LLM clients (only needed if you run llm_zero_shot.py)
anthropic>=0.34
openai>=1.30

# IMPORTANT for Windows + CUDA:
# Install torch separately to match your CUDA version, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Then `pip install -r requirements.txt` for the rest.
